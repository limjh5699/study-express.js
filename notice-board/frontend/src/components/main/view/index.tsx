import styled from "styled-components";

const View = () => {
  return (
    <Layout>
      <div></div>
    </Layout>
  );
};

const Layout = styled.div`
  width: 50%;
  height: 1000px;
  /* height: 100vh; */
  margin: 0 auto;
  background-color: red;
`;

// const;

export default View;
